
  # Kompas Geolocation Interface

  This is a code bundle for Kompas Geolocation Interface. The original project is available at https://www.figma.com/design/z4irwxJDn3UnzBae3e4y22/Kompas-Geolocation-Interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  